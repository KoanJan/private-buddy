appId: com.private-buddy.app
productName: Private Buddy
directories:
  output: dist-electron
files:
  - electron/dist/**/*
  - web-dist/**/*
mac:
  target:
    - dmg
    - zip
  category: public.app-category.productivity
  icon: build/icon.icns
  hardenedRuntime: true
  extraResources:
    - from: server_go/private-buddy-server
      to: go-server/private-buddy-server
    - from: server_go/.env
      to: go-server/.env
  extendInfo:
    NSMicrophoneUsageDescription: "Private Buddy does not use the microphone."
    NSCameraUsageDescription: "Private Buddy does not use the camera."
win:
  target:
    - target: nsis
      arch:
        - x64
  icon: build/icon.ico
  extraResources:
    - from: server_go/private-buddy-server.exe
      to: go-server/private-buddy-server.exe
    - from: server_go/.env
      to: go-server/.env
linux:
  target:
    - AppImage
    - deb
  icon: build/icon-1024.png
  category: Utility
  extraResources:
    - from: server_go/private-buddy-server
      to: go-server/private-buddy-server
    - from: server_go/.env
      to: go-server/.env
